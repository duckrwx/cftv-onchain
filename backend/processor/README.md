# Processor simulator

This folder will host the off-chain processor simulator that receives authenticated camera input, generates canonical event payloads, and prepares transactions for the custody ledger.
