{
  "name": "cftv-custody-mvp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-anchor.js --network sepolia",
    "anchor:sepolia": "hardhat run scripts/anchor-package.js --network sepolia",
    "read:sepolia": "hardhat run scripts/read-anchor.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
